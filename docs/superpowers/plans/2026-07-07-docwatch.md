# docwatch Implementation Plan (OSS 개정판)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Supersedes:** `2026-07-03-docview.md` (embed 모델 기반 — OSS 피벗으로 개정). 완료된 작업: 원판 Task 1 스캐폴드 (`2511fb4` + Astro 5 마이그레이션 `f65b4dd`, typecheck/build 통과). 이 개정판의 태스크는 그 위에서 시작한다.
>
> **기준 설계**: `~/.gstack/projects/ysj/ysj-docview-oss-design-20260707-160500.md` (APPROVED, 적대적 리뷰 9/10) + 스파이크 결과 `/home/ysj/docview/.spike/SPIKE-REPORT.md` (VIABLE).

**Goal:** `npx docwatch` 한 줄로 아무 Claude Code 프로젝트의 문서·계획·진행률을 라이브 관전하는 로컬 뷰어를 만들어 GitHub+npm에 오픈소스로 출시한다.

**Architecture:** npm 패키지가 Astro 5 + Starlight 프로젝트 전체를 내장(사전 번들)하고, `bin/cli.mjs`가 target 절대경로를 `DOCWATCH_ROOT` env로 넘겨 패키지 내부의 `astro dev`를 spawn한다. Astro 5 content-layer glob loader가 외부 base를 직접 읽고 **자체 워칭**한다(스파이크 검증 — 커스텀 chokidar 불필요). 대시보드(타임라인+카테고리 카드+Live Activity 배너)는 SSR + 5초 폴링. mermaid는 클라이언트 사이드 렌더.

**Tech Stack:** Node 18+, Astro ^5.18, Starlight ^0.30, TypeScript 5 strict, zod 3, mermaid (client ESM), Vitest 1.x, Playwright 1.40+, chokidar 3 (git 상태 감시 전용).

## Global Constraints

- **패키지명**: `docwatch` (npm 확정, 퍼블리시 직전 재확인). 로컬 디렉터리는 `/home/ysj/docview` 유지 (T14에서 package.json name만 변경).
- **Node floor**: `>=18.0.0` · **Astro major**: `^5` · **TS strict ON**
- **Target 루트 결정**: env `DOCWATCH_ROOT` (절대경로) — CLI가 설정. 코드에서 `cwd/..` 가정 금지.
- **Entry id 정규화 (스파이크 gotcha #1)**: glob loader는 id에서 소문자화 + 확장자 제거 + **leading dot 제거** (`.claude/plans/plan-a.md` → `claude/plans/plan-a`). 원본 경로가 필요하면 `entry.filePath` 사용.
- **`entry.filePath`는 viewer 루트 기준 상대경로 (gotcha #2)** — 절대경로 필요 시 반드시 `path.resolve(astroProjectRoot, entry.filePath)`.
- **mermaid**: 클라이언트 사이드만. rehype-mermaid/서버 렌더 금지 (headless Chromium 요구 때문).
- **FilterPanel 없음** — v1.1로 이연 (MVP Scope 확정).
- **체크박스 문법**: `- [ ]` / `- [x]`. 정규식은 `/^\s*[-*+]\s+\[([ xX])\]\s+/` 형태 — 이중 이스케이프(`\\[`) 금지 (스파이크에서 실제 발생한 버그 클래스).
- **커밋 prefix**: `feat|fix|test|docs|chore|refactor|build` · **태스크마다 커밋 1개 이상** · TDD Iron Law (스캐폴딩·순수 마크업 태스크는 명시된 경우 예외)
- **활동 폴링 간격**: 5000ms 고정 · **dev 포트**: 4321 기본, 점유 시 Astro 자동 이동 — **CLI는 로그에서 실제 포트를 파싱해야 함 (gotcha #5)**

## File Structure Map

| 파일 | 책임 | 상태 |
|---|---|---|
| `bin/cli.mjs` | npx 엔트리: target 해석→DOCWATCH_ROOT→astro dev spawn→포트 파싱→(옵션) 브라우저 오픈 | 신규 |
| `docwatch.config.ts` | 사용자 오버라이드 (include/exclude/categories) | 신규 |
| `src/data/config.ts` | 타입·기본값·병합·로드 | 신규 |
| `src/data/paths.ts` | DOCWATCH_ROOT 해석, filePath 절대화, entry-id↔원본경로 매핑 | 신규 |
| `src/data/categories.ts` | 카테고리 규칙 매칭 + 폴백 | 신규 |
| `src/data/scan.ts` | git log·dirty·체크박스·카테고리 집계 | 신규 |
| `src/data/activity.ts` | Live activity 컴포저 | 신규 |
| `src/data/git-watch.ts` | chokidar로 `.git/HEAD`·index 감시 → git 캐시 무효화 (콘텐츠 워칭은 glob loader가 담당) | 신규 |
| `src/content.config.ts` | Astro 5 위치. glob loader, base=DOCWATCH_ROOT | 기존 stub 교체 |
| `src/components/{RecentTimeline,CategoryCard,LiveActivityBanner,Mermaid}.astro` | UI | 신규 |
| `src/pages/{index,dashboard}.astro`, `src/pages/api/activity.json.ts` | 라우트 | 신규 |
| `src/layouts/DashboardLayout.astro` | 대시보드 레이아웃 | 신규 |
| `tests/…` | unit/integration/e2e (fixture 스크립트 포함) | 신규 |
| `README.md` `LICENSE` `CONTRIBUTING.md` `CHANGELOG.md` `.github/…` | OSS 위생 + CI | 신규 |

삭제 예정: `src/content/config.ts` (구 위치 stub), `src/content/docs/index.md` (T6에서).

---

### Task 1: Config 모듈 (docwatch.config.ts 로드·병합)

**Files:** Create `src/data/config.ts`, `tests/unit/config.test.ts`, `docwatch.config.ts`

**Interfaces (Produces):**
- `type CategoryRule = { name: string; match: string | string[]; icon?: string }`
- `type DocwatchConfig = { include: string[]; exclude: string[]; categories: CategoryRule[] }`
- `const DEFAULT_CONFIG: DocwatchConfig`
- `function mergeConfig(override: Partial<DocwatchConfig>): DocwatchConfig`

- [ ] **Step 1: 실패 테스트** (`tests/unit/config.test.ts`)

```ts
import { describe, it, expect } from 'vitest';
import { DEFAULT_CONFIG, mergeConfig, type DocwatchConfig } from '@/data/config';

describe('mergeConfig', () => {
  it('빈 오버라이드는 기본값 그대로', () => {
    expect(mergeConfig({})).toEqual(DEFAULT_CONFIG);
  });
  it('include 교체', () => {
    expect(mergeConfig({ include: ['x/**/*.md'] }).include).toEqual(['x/**/*.md']);
  });
  it('동명 카테고리는 오버라이드가 승리', () => {
    const merged = mergeConfig({ categories: [{ name: 'ADR', match: 'my/**', icon: '🅰' }] });
    expect(merged.categories.find(c => c.name === 'ADR')).toEqual({ name: 'ADR', match: 'my/**', icon: '🅰' });
  });
  it('오버라이드 안 된 기본 카테고리 보존', () => {
    const merged = mergeConfig({ categories: [{ name: 'ADR', match: 'x' }] });
    expect(merged.categories.some(c => c.name === 'PRD')).toBe(true);
    expect(merged.categories.some(c => c.name === 'Plans')).toBe(true);
  });
});
```

- [ ] **Step 2: 실행 → FAIL 확인** — `npm run test:unit -- tests/unit/config.test.ts` → "Cannot find module '@/data/config'"

- [ ] **Step 3: 구현** (`src/data/config.ts`)

```ts
export type CategoryRule = { name: string; match: string | string[]; icon?: string };
export type DocwatchConfig = { include: string[]; exclude: string[]; categories: CategoryRule[] };

export const DEFAULT_CONFIG: DocwatchConfig = {
  include: ['docs/**/*.md', 'AGENTS.md', 'README.md', 'CLAUDE.md', '.omc/**/*.md', '.claude/plans/*.md'],
  exclude: ['**/node_modules/**', '.git/**', '.docwatch-cache/**'],
  categories: [
    { name: 'ADR',   match: 'docs/adr/**',                            icon: '📐' },
    { name: 'PRD',   match: ['docs/prd*', '.omc/prd-*'],              icon: '📋' },
    { name: 'Plans', match: '.claude/plans/**',                       icon: '🗺' },
    { name: 'Root',  match: ['AGENTS.md', 'README.md', 'CLAUDE.md'],  icon: '📄' },
  ],
};

export function mergeConfig(override: Partial<DocwatchConfig>): DocwatchConfig {
  const overridden = new Set((override.categories ?? []).map(c => c.name));
  return {
    include: override.include ?? DEFAULT_CONFIG.include,
    exclude: override.exclude ?? DEFAULT_CONFIG.exclude,
    categories: [
      ...DEFAULT_CONFIG.categories.filter(c => !overridden.has(c.name)),
      ...(override.categories ?? []),
    ],
  };
}

export async function loadConfig(rootDir: string): Promise<DocwatchConfig> {
  const path = await import('node:path');
  const fs = await import('node:fs/promises');
  const p = path.join(rootDir, 'docwatch.config.ts');
  try { await fs.access(p); } catch { return DEFAULT_CONFIG; }
  const mod = await import(/* @vite-ignore */ p);
  return mergeConfig((mod.default ?? {}) as Partial<DocwatchConfig>);
}
```

- [ ] **Step 4: 실행 → PASS (4건)**
- [ ] **Step 5: 사용자 오버라이드 파일** (`docwatch.config.ts` 루트)

```ts
import type { DocwatchConfig } from './src/data/config';
export default {
  // 기본값은 src/data/config.ts 참조. 필요한 항목만 오버라이드.
} satisfies Partial<DocwatchConfig>;
```

- [ ] **Step 6: 커밋** — `feat: docwatch config types, defaults, merge`

---

### Task 2: 경로 헬퍼 + 카테고리 해석 (DOCWATCH_ROOT 모델)

**Files:** Create `src/data/paths.ts`, `src/data/categories.ts`, `tests/unit/paths.test.ts`, `tests/unit/categories.test.ts`

**Interfaces (Produces):**
- `function targetRepoRoot(): string` — `DOCWATCH_ROOT` env 필수, 없으면 throw (CLI가 항상 설정; dev 편의로 `process.env.DOCWATCH_ROOT ?? process.cwd()` 허용하되 경고)
- `function absFromEntry(filePath: string, astroRoot: string): string` — 상대 filePath 절대화 (gotcha #2)
- `function idToSourceRel(id: string, includeRoots: string[]): string | null` — 정규화된 entry id → target 루트 기준 원본 상대경로 추정 (leading-dot 복원: `claude/…`→`.claude/…`, `omc/…`→`.omc/…`)
- `function resolveCategory(sourceRelPath: string, rules: CategoryRule[]): string` — 첫 매치 승리, 폴백 = 첫 경로 세그먼트(루트 파일은 `Root`)

- [ ] **Step 1: 실패 테스트** (`tests/unit/paths.test.ts`)

```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { targetRepoRoot, absFromEntry, idToSourceRel } from '@/data/paths';

describe('targetRepoRoot', () => {
  const saved = process.env.DOCWATCH_ROOT;
  afterEach(() => { process.env.DOCWATCH_ROOT = saved; });
  it('DOCWATCH_ROOT 절대경로 반환', () => {
    process.env.DOCWATCH_ROOT = '/tmp/x';
    expect(targetRepoRoot()).toBe('/tmp/x');
  });
});

describe('absFromEntry', () => {
  it('상대 filePath를 astro 루트 기준 절대화', () => {
    expect(absFromEntry('../target/docs/a.md', '/pkg/viewer')).toBe('/pkg/target/docs/a.md');
  });
  it('절대 filePath는 그대로', () => {
    expect(absFromEntry('/abs/docs/a.md', '/pkg/viewer')).toBe('/abs/docs/a.md');
  });
});

describe('idToSourceRel', () => {
  const roots = ['docs', '.claude', '.omc'];
  it('leading dot 복원: claude → .claude', () => {
    expect(idToSourceRel('claude/plans/plan-a', roots)).toBe('.claude/plans/plan-a.md');
  });
  it('dot 없는 루트는 그대로', () => {
    expect(idToSourceRel('docs/adr/adr-001', roots)).toBe('docs/adr/adr-001.md');
  });
  it('루트 단일 파일: readme → README.md는 매핑 불가 → null (filePath 사용 강제)', () => {
    expect(idToSourceRel('readme', roots)).toBeNull();
  });
});
```

- [ ] **Step 2: 실행 → FAIL** — 모듈 없음
- [ ] **Step 3: 구현** (`src/data/paths.ts`)

```ts
import path from 'node:path';

export function targetRepoRoot(): string {
  const r = process.env.DOCWATCH_ROOT;
  if (r) return path.resolve(r);
  console.warn('[docwatch] DOCWATCH_ROOT unset — falling back to cwd (dev only)');
  return process.cwd();
}

export function absFromEntry(filePath: string, astroRoot: string): string {
  return path.isAbsolute(filePath) ? filePath : path.resolve(astroRoot, filePath);
}

/** 정규화 id → 원본 상대경로 추정. 대소문자 변형·루트 파일은 null (호출측은 filePath 우선). */
export function idToSourceRel(id: string, includeRoots: string[]): string | null {
  const seg = id.split('/');
  if (seg.length === 1) return null; // 루트 파일(readme 등)은 원본 대소문자 복원 불가
  const dotted = '.' + seg[0];
  const root = includeRoots.includes(dotted) ? dotted : includeRoots.includes(seg[0]) ? seg[0] : null;
  if (!root) return null;
  return [root, ...seg.slice(1)].join('/') + '.md';
}
```

- [ ] **Step 4: 카테고리 테스트** (`tests/unit/categories.test.ts`)

```ts
import { describe, it, expect } from 'vitest';
import { resolveCategory } from '@/data/categories';
import type { CategoryRule } from '@/data/config';

const RULES: CategoryRule[] = [
  { name: 'ADR',   match: 'docs/adr/**' },
  { name: 'PRD',   match: ['docs/prd*', '.omc/prd-*'] },
  { name: 'Plans', match: '.claude/plans/**' },
  { name: 'Root',  match: ['AGENTS.md', 'README.md', 'CLAUDE.md'] },
];

describe('resolveCategory', () => {
  it('순서 우선 첫 매치', () => expect(resolveCategory('docs/adr/x.md', RULES)).toBe('ADR'));
  it('배열 glob', () => expect(resolveCategory('.omc/prd-yt.md', RULES)).toBe('PRD'));
  it('딥 glob', () => expect(resolveCategory('.claude/plans/p.md', RULES)).toBe('Plans'));
  it('루트 정확 매치', () => expect(resolveCategory('AGENTS.md', RULES)).toBe('Root'));
  it('폴백: 첫 세그먼트', () => expect(resolveCategory('apps/b/AGENTS.md', RULES)).toBe('apps'));
  it('루트 무매치 파일 → Root', () => expect(resolveCategory('notes.md', RULES)).toBe('Root'));
});
```

- [ ] **Step 5: 구현** (`src/data/categories.ts`)

```ts
import type { CategoryRule } from './config';

function globToRegex(glob: string): RegExp {
  let re = '^'; let i = 0;
  while (i < glob.length) {
    const c = glob[i];
    if (c === '*' && glob[i + 1] === '*') { re += '.*'; i += 2; if (glob[i] === '/') i++; }
    else if (c === '*') { re += '[^/]*'; i++; }
    else if (c === '?') { re += '[^/]'; i++; }
    else if ('.^$+(){}|\\'.includes(c)) { re += '\\' + c; i++; }
    else { re += c; i++; }
  }
  return new RegExp(re + '$');
}

export function resolveCategory(sourceRelPath: string, rules: CategoryRule[]): string {
  for (const rule of rules) {
    const pats = Array.isArray(rule.match) ? rule.match : [rule.match];
    if (pats.some(p => globToRegex(p).test(sourceRelPath))) return rule.name;
  }
  const first = sourceRelPath.split('/')[0];
  return first === sourceRelPath ? 'Root' : first;
}
```

- [ ] **Step 6: 전체 유닛 실행 → PASS** — `npm run test:unit`
- [ ] **Step 7: 커밋** — `feat: DOCWATCH_ROOT path model + category resolution`

---

### Task 3: 체크박스 스캐너

**Files:** Create `src/data/scan.ts`(시작), `tests/unit/scan-checkbox.test.ts`

**Interfaces (Produces):** `function scanCheckboxes(markdown: string): { total: number; checked: number }` — 코드펜스/인라인코드 내부 무시, 중첩 카운트, CRLF 허용.

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, it, expect } from 'vitest';
import { scanCheckboxes } from '@/data/scan';

describe('scanCheckboxes', () => {
  it('빈 문자열', () => expect(scanCheckboxes('')).toEqual({ total: 0, checked: 0 }));
  it('평면 혼합', () => expect(scanCheckboxes('- [x] a\n- [ ] b\n- [X] c')).toEqual({ total: 3, checked: 2 }));
  it('중첩', () => expect(scanCheckboxes('- [x] a\n  - [ ] a1\n  - [x] a2')).toEqual({ total: 3, checked: 2 }));
  it('코드펜스 무시', () => {
    const md = ['- [x] real', '```', '- [ ] fake', '```', '- [ ] real2'].join('\n');
    expect(scanCheckboxes(md)).toEqual({ total: 2, checked: 1 });
  });
  it('인라인 코드 무시', () => expect(scanCheckboxes('use `- [ ]`\n- [x] done')).toEqual({ total: 1, checked: 1 }));
  it('CRLF', () => expect(scanCheckboxes('- [x] a\r\n- [ ] b')).toEqual({ total: 2, checked: 1 }));
  it('이중 이스케이프 회귀 방지: 리터럴 백슬래시 라인은 미매치', () =>
    expect(scanCheckboxes('- \\[x\\] not-a-box')).toEqual({ total: 0, checked: 0 }));
});
```

- [ ] **Step 2: FAIL 확인 → Step 3: 구현**

```ts
const CHECKBOX_LINE = /^\s*[-*+]\s+\[([ xX])\]\s+/;

export function scanCheckboxes(markdown: string): { total: number; checked: number } {
  let total = 0, checked = 0, inFence = false;
  for (const raw of markdown.replace(/\r\n/g, '\n').split('\n')) {
    const line = raw.replace(/`[^`\n]*`/g, '');
    if (/^\s*```/.test(line)) { inFence = !inFence; continue; }
    if (inFence) continue;
    const m = CHECKBOX_LINE.exec(line);
    if (m) { total++; if (m[1] !== ' ') checked++; }
  }
  return { total, checked };
}
```

- [ ] **Step 4: PASS (7건) → Step 5: 커밋** — `feat: checkbox scanner with fence/inline/escape guards`

---

### Task 4: 픽스처 레포 + git log·dirty 스캐너 (통합)

**Files:** Create `tests/fixtures/setup-sample-repo.sh`, `tests/integration/scan-git.test.ts` · Extend `src/data/scan.ts`

**Interfaces (Produces):**
- `type Commit = { sha: string; author: string; relTime: string; subject: string; files: string[] }`
- `function scanGitLog(rootDir: string, limit?: number): Promise<Commit[]>`
- `type DirtyEntry = { path: string; status: 'M'|'A'|'D'|'??'|'R' }`
- `function scanDirtyFiles(rootDir: string): Promise<DirtyEntry[]>`

- [ ] **Step 1: 픽스처 스크립트** (`tests/fixtures/setup-sample-repo.sh`, `chmod +x`)

```bash
#!/usr/bin/env bash
set -euo pipefail
DEST="${1:-tests/fixtures/sample-repo}"
rm -rf "$DEST"; mkdir -p "$DEST/docs/adr"; cd "$DEST"
git init -q -b main
git config user.email test@example.com && git config user.name test
echo "# root" > README.md;                       git add . && git commit -q -m "chore: init"
echo "# ADR-001" > docs/adr/ADR-001.md;          git add . && git commit -q -m "docs: add ADR-001"
printf -- "- [x] foo\n" > docs/adr/ADR-002.md;   git add . && git commit -q -m "docs: add ADR-002"
echo "# uncommitted" > NOTES.md   # dirty로 남김
```

- [ ] **Step 2: 실패 테스트** (`tests/integration/scan-git.test.ts`)

```ts
import { describe, it, expect, beforeAll } from 'vitest';
import { execSync } from 'node:child_process';
import path from 'node:path';
import { scanGitLog, scanDirtyFiles } from '@/data/scan';

const FIXTURE = path.resolve(__dirname, '../fixtures/sample-repo');
beforeAll(() => {
  execSync(`bash ${path.resolve(__dirname, '../fixtures/setup-sample-repo.sh')} ${FIXTURE}`, { stdio: 'inherit' });
});

it('역시간순 커밋 + 파일 목록', async () => {
  const commits = await scanGitLog(FIXTURE, 10);
  expect(commits.length).toBe(3);
  expect(commits[0].subject).toContain('ADR-002');
  expect(commits[0].files).toContain('docs/adr/ADR-002.md');
});
it('미커밋 NOTES.md 감지', async () => {
  const dirty = await scanDirtyFiles(FIXTURE);
  expect(dirty.find(d => d.path === 'NOTES.md')?.status).toBe('??');
});
```

- [ ] **Step 3: FAIL → Step 4: 구현** (`src/data/scan.ts`에 append)

```ts
import { execFile as execFileCb } from 'node:child_process';
import { promisify } from 'node:util';
const execFile = promisify(execFileCb);

export type Commit = { sha: string; author: string; relTime: string; subject: string; files: string[] };

export async function scanGitLog(rootDir: string, limit = 50): Promise<Commit[]> {
  const SEP = '|~dw~|';
  const { stdout } = await execFile('git',
    ['-C', rootDir, 'log', `--pretty=format:%x00%H${SEP}%an${SEP}%ar${SEP}%s`, '--name-only', `-n${limit}`],
    { maxBuffer: 32 * 1024 * 1024 });
  return stdout.split('\0').filter(Boolean).map(chunk => {
    const [meta, ...fileLines] = chunk.trim().split('\n');
    const [sha, author, relTime, subject] = meta.split(SEP);
    return { sha, author, relTime, subject, files: fileLines.map(f => f.trim()).filter(Boolean) };
  });
}

export type DirtyEntry = { path: string; status: 'M'|'A'|'D'|'??'|'R' };

export async function scanDirtyFiles(rootDir: string): Promise<DirtyEntry[]> {
  const { stdout } = await execFile('git', ['-C', rootDir, 'status', '--porcelain', '-z'], { maxBuffer: 8 * 1024 * 1024 });
  return stdout.split('\0').filter(Boolean).map(p => {
    const code = p.slice(0, 2).trim();
    return { path: p.slice(3), status: (code[0] === '?' ? '??' : code[0]) as DirtyEntry['status'] };
  });
}
```

- [ ] **Step 5: PASS 확인 → Step 6: `.gitignore`에 `tests/fixtures/sample-repo/` 추가 → Step 7: 커밋** — `feat: git log + dirty scanners with fixture repo`

---

### Task 5: 카테고리 집계 + Live Activity 컴포저

**Files:** Extend `src/data/scan.ts` (aggregate) · Create `src/data/activity.ts`, `tests/unit/aggregate.test.ts`, `tests/integration/activity.test.ts` · `npm install fast-glob@^3`

**Interfaces (Produces):**
- `type Aggregate = { category: string; docCount: number; total: number; checked: number; percent: number }`
- `function aggregateByCategory(entries: Array<{ sourceRelPath: string; content: string }>, rules: CategoryRule[]): Aggregate[]`
- `type Activity = { dirty: DirtyEntry[]; recentlyModified: Array<{path:string;mtime:number}>; activePlan: {path:string;title:string}|null; generatedAt: number }`
- `async function getActivity(rootDir: string, cfg: DocwatchConfig): Promise<Activity>` — 스펙 의미론: dirty=전체 git status / recent=include 매치 & mtime≤5분, 최대 10 / activePlan=(a) `.claude/plans` mtime≤30분 최신 → (b) include 매치 mtime≤30분 최신 → (c) null

- [ ] **Step 1: aggregate 실패 테스트** (`tests/unit/aggregate.test.ts`)

```ts
import { describe, it, expect } from 'vitest';
import { aggregateByCategory } from '@/data/scan';
import type { CategoryRule } from '@/data/config';

const RULES: CategoryRule[] = [{ name: 'ADR', match: 'docs/adr/**' }, { name: 'Plans', match: '.claude/plans/**' }];

it('그룹·합산·백분율', () => {
  const agg = aggregateByCategory([
    { sourceRelPath: 'docs/adr/a.md', content: '- [x] a\n- [ ] b' },
    { sourceRelPath: 'docs/adr/b.md', content: '- [x] c' },
    { sourceRelPath: '.claude/plans/p.md', content: '- [ ] x\n- [ ] y' },
  ], RULES);
  const adr = agg.find(a => a.category === 'ADR')!;
  expect(adr).toMatchObject({ docCount: 2, total: 3, checked: 2 });
  expect(adr.percent).toBeCloseTo(200 / 3, 1);
  expect(agg.find(a => a.category === 'Plans')!.percent).toBe(0);
});
it('체크박스 0개 → percent 0', () => {
  expect(aggregateByCategory([{ sourceRelPath: 'docs/adr/x.md', content: '# none' }], RULES)[0].percent).toBe(0);
});
```

- [ ] **Step 2: FAIL → 구현** (`src/data/scan.ts` append)

```ts
import { resolveCategory } from './categories';
import type { CategoryRule } from './config';

export type Aggregate = { category: string; docCount: number; total: number; checked: number; percent: number };

export function aggregateByCategory(
  entries: Array<{ sourceRelPath: string; content: string }>, rules: CategoryRule[],
): Aggregate[] {
  const buckets = new Map<string, Aggregate>();
  for (const e of entries) {
    const cat = resolveCategory(e.sourceRelPath, rules);
    const b = buckets.get(cat) ?? { category: cat, docCount: 0, total: 0, checked: 0, percent: 0 };
    const { total, checked } = scanCheckboxes(e.content);
    b.docCount++; b.total += total; b.checked += checked;
    buckets.set(cat, b);
  }
  return [...buckets.values()].map(b => ({ ...b, percent: b.total ? (b.checked / b.total) * 100 : 0 }));
}
```

- [ ] **Step 3: activity 실패 테스트** (`tests/integration/activity.test.ts` — tmp 레포 즉석 생성)

```ts
import { describe, it, expect, beforeAll } from 'vitest';
import { execSync } from 'node:child_process';
import fs from 'node:fs/promises';
import path from 'node:path';
import os from 'node:os';
import { getActivity } from '@/data/activity';
import { mergeConfig } from '@/data/config';

let root: string;
beforeAll(async () => {
  root = await fs.mkdtemp(path.join(os.tmpdir(), 'dw-act-'));
  execSync('git init -q -b main && git config user.email t@e.com && git config user.name t', { cwd: root });
  await fs.mkdir(path.join(root, '.claude/plans'), { recursive: true });
  await fs.writeFile(path.join(root, 'README.md'), '# root');
  await fs.writeFile(path.join(root, '.claude/plans/active.md'), '# Active plan\n- [ ] task');
  execSync('git add . && git commit -q -m init', { cwd: root });
  await fs.writeFile(path.join(root, 'NOTES.md'), 'dirty');
});

it('dirty·recent·activePlan 반환', async () => {
  const act = await getActivity(root, mergeConfig({}));
  expect(act.dirty.some(d => d.path === 'NOTES.md')).toBe(true);
  expect(act.recentlyModified.length).toBeGreaterThan(0);
  expect(act.activePlan?.path).toContain('.claude/plans/active.md');
  expect(act.activePlan?.title).toBe('Active plan');
});
```

- [ ] **Step 4: FAIL → 구현** (`src/data/activity.ts`)

```ts
import path from 'node:path';
import fs from 'node:fs/promises';
import fg from 'fast-glob';
import { scanDirtyFiles, type DirtyEntry } from './scan';
import type { DocwatchConfig } from './config';

export type Activity = {
  dirty: DirtyEntry[];
  recentlyModified: Array<{ path: string; mtime: number }>;
  activePlan: { path: string; title: string } | null;
  generatedAt: number;
};

const FIVE_MIN = 300_000, THIRTY_MIN = 1_800_000;

async function firstHeading(abs: string): Promise<string> {
  try {
    const m = (await fs.readFile(abs, 'utf8')).match(/^#\s+(.+)$/m);
    return m ? m[1].trim() : path.basename(abs, '.md');
  } catch { return path.basename(abs); }
}

export async function getActivity(rootDir: string, cfg: DocwatchConfig): Promise<Activity> {
  const now = Date.now();
  const [dirty, absPaths] = await Promise.all([
    scanDirtyFiles(rootDir).catch(() => [] as DirtyEntry[]),
    fg(cfg.include, { cwd: rootDir, absolute: true, ignore: cfg.exclude, dot: true }),
  ]);
  const stats = (await Promise.all(absPaths.map(async a => {
    const s = await fs.stat(a).catch(() => null);
    return s ? { rel: path.relative(rootDir, a).split(path.sep).join('/'), mtime: s.mtimeMs, abs: a } : null;
  }))).filter((x): x is NonNullable<typeof x> => !!x);

  const recentlyModified = stats.filter(m => now - m.mtime <= FIVE_MIN)
    .sort((a, b) => b.mtime - a.mtime).slice(0, 10)
    .map(({ rel, mtime }) => ({ path: rel, mtime }));

  const fresh = (xs: typeof stats) => xs.filter(m => now - m.mtime <= THIRTY_MIN).sort((a, b) => b.mtime - a.mtime);
  const chosen = fresh(stats.filter(m => m.rel.startsWith('.claude/plans/')))[0] ?? fresh(stats)[0] ?? null;

  return {
    dirty, recentlyModified, generatedAt: now,
    activePlan: chosen ? { path: chosen.rel, title: await firstHeading(chosen.abs) } : null,
  };
}
```

- [ ] **Step 5: 전체 테스트 PASS → Step 6: 커밋** — `feat: category aggregate + live activity composer`

---

### Task 6: Content Collection — 외부 루트 glob loader (스파이크 패턴 채택)

**Files:** Create `src/content.config.ts` (Astro 5 위치) · Delete `src/content/config.ts`, `src/content/docs/index.md` (구 stub) · Modify `astro.config.mjs`

수동 검증 태스크 (Astro 런타임 결합 — vitest 유닛은 비실용, 스킬의 "TDD when practical" 예외 명시).

- [ ] **Step 1: 구 stub 삭제** — `git rm src/content/config.ts src/content/docs/index.md`
- [ ] **Step 2: `src/content.config.ts` 작성** (스파이크 검증 패턴)

```ts
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';
import { DEFAULT_CONFIG } from './data/config';
import { targetRepoRoot } from './data/paths';

const docs = defineCollection({
  loader: glob({
    base: targetRepoRoot(),          // 절대 외부 경로 — 스파이크 TEST 1 PASS 패턴
    pattern: DEFAULT_CONFIG.include,
  }),
  schema: z.object({
    title: z.string().optional(),
    description: z.string().optional(),
    status: z.enum(['draft', 'in-progress', 'done']).optional(),
    category: z.string().optional(),
    tags: z.array(z.string()).optional(),
  }),
});

export const collections = { docs };
```

- [ ] **Step 3: `astro.config.mjs`에 `site` 추가** (Task1 재실행의 sitemap 경고 해소) — `defineConfig({ site: 'http://localhost:4321', ... })`
- [ ] **Step 4: 스모크** — `.spike/target`을 재활용: `DOCWATCH_ROOT=$PWD/.spike/target npx astro dev --port 4321` → `curl -sf localhost:4321/docs/adr/adr-001 | grep -q "Spike ADR"` → OK 확인 → 서버 종료 (`pkill -f "ast[r]o dev"` 브래킷 패턴)
- [ ] **Step 5: 커밋** — `feat: content collection reads external DOCWATCH_ROOT`

---

### Task 7: git 상태 워처 (범위 축소판)

**Files:** Create `src/data/git-watch.ts` · Modify `astro.config.mjs` (integration hook)

콘텐츠 워칭은 glob loader가 자체 수행 (스파이크 TEST 3a) — **이 태스크는 `.git/HEAD`·`.git/index`만 감시**해 git 캐시 무효화 신호를 보낸다. 수동 검증 태스크.

- [ ] **Step 1: 구현** (`src/data/git-watch.ts`)

```ts
import chokidar from 'chokidar';
import path from 'node:path';

export function startGitWatch(rootDir: string, onGitChange: () => void) {
  const watcher = chokidar.watch(
    [path.join(rootDir, '.git/HEAD'), path.join(rootDir, '.git/index')],
    { ignoreInitial: true, followSymlinks: false, awaitWriteFinish: { stabilityThreshold: 200, pollInterval: 50 } },
  );
  watcher.on('all', () => onGitChange());
  return { close: () => watcher.close() };
}
```

- [ ] **Step 2: astro.config.mjs에 integration 등록**

```js
import { targetRepoRoot } from './src/data/paths.ts';
import { startGitWatch } from './src/data/git-watch.ts';

const gitWatchIntegration = {
  name: 'docwatch-git-watch',
  hooks: {
    'astro:server:setup': ({ server }) => {
      const w = startGitWatch(targetRepoRoot(), () => server.ws.send({ type: 'full-reload', path: '*' }));
      server.httpServer?.once('close', () => w.close());
    },
  },
};
// integrations 배열에 gitWatchIntegration 추가
```

- [ ] **Step 3: 수동 검증** — dev 가동 → `.spike/target`에서 `git commit --allow-empty -m tick` → 브라우저/로그 full-reload 확인 → 서버 종료
- [ ] **Step 4: 커밋** — `feat: git-state watcher (content watching handled by glob loader)`

---

### Task 8: 대시보드 셸 + 리다이렉트 + 레이아웃

**Files:** Create `src/pages/index.astro`, `src/pages/dashboard.astro`, `src/layouts/DashboardLayout.astro`

마크업 태스크 (E2E가 Task 15에서 검증). 원판 Task 9와 동일 코드 — `src/pages/index.astro`:

```astro
---
return Astro.redirect('/dashboard');
---
```

`src/layouts/DashboardLayout.astro`:

```astro
---
const { title = 'docwatch' } = Astro.props;
---
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" /><meta name="viewport" content="width=device-width" />
    <title>{title}</title>
    <style>
      body { font: 14px/1.5 system-ui, sans-serif; margin: 0; padding: 0 24px 32px; }
      main { max-width: 1200px; margin: 0 auto; }
      .banner { border: 1px solid #ddd; border-radius: 8px; padding: 12px; margin: 16px 0; }
      .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
      @media (max-width: 800px) { .grid { grid-template-columns: 1fr; } }
      .col { border: 1px solid #eee; border-radius: 8px; padding: 12px; }
      h1 { font-size: 20px; margin: 16px 0 8px; }
      h2 { font-size: 14px; margin: 0 0 8px; color: #555; }
    </style>
  </head>
  <body><main><h1>docwatch</h1><slot /></main></body>
</html>
```

`src/pages/dashboard.astro` (플레이스홀더 3구역 — Task 9-12에서 채움):

```astro
---
import DashboardLayout from '@/layouts/DashboardLayout.astro';
---
<DashboardLayout>
  <section class="banner" id="live-activity"><h2>Live Activity</h2><p>(Task 12)</p></section>
  <div class="grid">
    <section class="col" id="timeline"><h2>Recent commits</h2><p>(Task 9)</p></section>
    <section class="col" id="cards"><h2>Categories</h2><p>(Task 10)</p></section>
  </div>
</DashboardLayout>
```

- [ ] 작성 → dev 스모크 (`curl -L localhost:4321/ | grep docwatch`) → 커밋 `feat: dashboard shell + redirect`

---

### Task 9: RecentTimeline 컴포넌트

**Files:** Create `src/components/RecentTimeline.astro` · Modify `src/pages/dashboard.astro`

```astro
---
import { scanGitLog, type Commit } from '@/data/scan';
import { targetRepoRoot } from '@/data/paths';
const commits: Commit[] = await scanGitLog(targetRepoRoot(), 30).catch(() => []);
const groups = new Map<string, Commit[]>();
for (const c of commits) { const l = groups.get(c.relTime) ?? []; l.push(c); groups.set(c.relTime, l); }
---
<div>
  {commits.length === 0 && <p style="color:#888">git 저장소가 아니거나 커밋이 없습니다.</p>}
  {[...groups.entries()].map(([label, list]) => (
    <div>
      <div style="font-weight:600;margin-top:8px;">{label}</div>
      <ul style="margin:4px 0 0 16px;padding:0;">
        {list.map(c => (
          <li><code>{c.sha.slice(0, 7)}</code> {c.subject}{c.files[0] && <span style="color:#888"> · {c.files[0]}</span>}</li>
        ))}
      </ul>
    </div>
  ))}
</div>
```

- [ ] dashboard.astro의 timeline 플레이스홀더 교체 (import + `<RecentTimeline />`) → 스모크 → 커밋 `feat: recent commits timeline`

---

### Task 10: CategoryCard + 대시보드 연결

**Files:** Create `src/components/CategoryCard.astro` · Modify `src/pages/dashboard.astro`

CategoryCard:

```astro
---
const { category, docCount, checked, total, percent, icon } = Astro.props;
---
<div style="border:1px solid #eee;border-radius:6px;padding:10px;margin:8px 0;">
  <div style="display:flex;justify-content:space-between;">
    <strong>{icon ?? '📄'} {category}</strong>
    <span style="color:#555">{Math.round(percent)}% · {docCount} docs</span>
  </div>
  <div style="background:#eee;height:6px;border-radius:3px;overflow:hidden;margin-top:6px;">
    <div style={`background:#4b7;height:100%;width:${Math.round(percent)}%`}></div>
  </div>
  <div style="color:#888;font-size:12px;margin-top:4px;">{checked}/{total} checkboxes</div>
</div>
```

dashboard.astro frontmatter — **filePath 절대화 필수 (gotcha #2)**:

```astro
---
import DashboardLayout from '@/layouts/DashboardLayout.astro';
import RecentTimeline from '@/components/RecentTimeline.astro';
import CategoryCard from '@/components/CategoryCard.astro';
import { getCollection } from 'astro:content';
import { aggregateByCategory } from '@/data/scan';
import { DEFAULT_CONFIG } from '@/data/config';
import { targetRepoRoot, absFromEntry } from '@/data/paths';
import fs from 'node:fs/promises';
import path from 'node:path';

const root = targetRepoRoot();
const astroRoot = path.resolve(new URL('../..', import.meta.url).pathname);
const docs = await getCollection('docs');
const entries = await Promise.all(docs.map(async d => {
  const abs = absFromEntry((d as any).filePath ?? d.id + '.md', astroRoot);
  return {
    sourceRelPath: path.relative(root, abs).split(path.sep).join('/'),
    content: await fs.readFile(abs, 'utf8').catch(() => ''),
  };
}));
const cats = aggregateByCategory(entries, DEFAULT_CONFIG.categories);
const iconOf = (n: string) => DEFAULT_CONFIG.categories.find(c => c.name === n)?.icon;
---
```

cards 구역: `{cats.map(c => <CategoryCard {...c} icon={iconOf(c.category)} />)}`

- [ ] 작성 → 스모크 (spike target 기준 ADR/Plans 카드 렌더 확인) → 커밋 `feat: category progress cards`

---

### Task 11: /api/activity.json 엔드포인트

**Files:** Create `src/pages/api/activity.json.ts`

```ts
import type { APIRoute } from 'astro';
import { getActivity } from '@/data/activity';
import { DEFAULT_CONFIG } from '@/data/config';
import { targetRepoRoot } from '@/data/paths';

export const prerender = false;

export const GET: APIRoute = async () => {
  const data = await getActivity(targetRepoRoot(), DEFAULT_CONFIG);
  return new Response(JSON.stringify(data), {
    headers: { 'content-type': 'application/json', 'cache-control': 'no-store' },
  });
};
```

- [ ] 작성 → 스모크: `curl -s localhost:4321/api/activity.json | python3 -m json.tool | head` → 4개 키 확인 → 커밋 `feat: activity endpoint`

---

### Task 12: LiveActivityBanner (SSR + 5초 폴링)

**Files:** Create `src/components/LiveActivityBanner.astro` · Modify `src/pages/dashboard.astro` (배너 교체)

```astro
---
import { getActivity, type Activity } from '@/data/activity';
import { DEFAULT_CONFIG } from '@/data/config';
import { targetRepoRoot } from '@/data/paths';
const initial: Activity = await getActivity(targetRepoRoot(), DEFAULT_CONFIG);
---
<div id="lab-root" data-initial={JSON.stringify(initial)}>
  <div><strong>Dirty</strong><ul id="lab-dirty" style="margin:4px 0 0 16px;"></ul></div>
  <div style="margin-top:8px;"><strong>Recently modified (5m)</strong><ul id="lab-recent" style="margin:4px 0 0 16px;"></ul></div>
  <div id="lab-plan" style="margin-top:8px;"></div>
</div>
<script>
  const root = document.getElementById('lab-root');
  function render(d) {
    const q = (id) => document.getElementById(id);
    q('lab-dirty').innerHTML = (d.dirty || []).slice(0, 20)
      .map(x => `<li><code>${x.status}</code> ${x.path}</li>`).join('') || '<li style="color:#888">clean</li>';
    q('lab-recent').innerHTML = (d.recentlyModified || [])
      .map(x => `<li>${x.path}</li>`).join('') || '<li style="color:#888">none</li>';
    q('lab-plan').innerHTML = d.activePlan
      ? `<strong>Active plan:</strong> ${d.activePlan.title} <span style="color:#888">(${d.activePlan.path})</span>`
      : '<span style="color:#888">no active plan</span>';
  }
  render(JSON.parse(root?.dataset.initial || '{}'));
  setInterval(async () => {
    try { const r = await fetch('/api/activity.json', { cache: 'no-store' }); if (r.ok) render(await r.json()); } catch {}
  }, 5000);
</script>
```

- [ ] 작성 + 대시보드 연결 → 수동 검증 (spike target에 파일 touch → 5초 내 recent 갱신) → 커밋 `feat: live activity banner`

---

### Task 13: 클라이언트 사이드 Mermaid

**Files:** Create `src/components/Mermaid.astro` · Modify Starlight/페이지 통합 지점 · `npm install mermaid@^11`

전략: md 코드펜스 ` ```mermaid `는 Astro 기본 렌더로 `<pre><code class="language-mermaid">`가 됨 → 클라이언트 스크립트가 이를 찾아 mermaid.render로 교체.

- [ ] **Step 1: 전역 스크립트 컴포넌트** (`src/components/Mermaid.astro`)

```astro
<script>
  import mermaid from 'mermaid';
  mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
  async function renderAll() {
    const blocks = document.querySelectorAll('pre > code.language-mermaid');
    let i = 0;
    for (const code of blocks) {
      const src = code.textContent ?? '';
      const holder = document.createElement('div');
      holder.className = 'mermaid-rendered';
      try {
        const { svg } = await mermaid.render(`mm-${i++}`, src);
        holder.innerHTML = svg;
        code.closest('pre')?.replaceWith(holder);
      } catch { /* 렌더 실패 시 원본 코드블록 유지 */ }
    }
  }
  renderAll();
  document.addEventListener('astro:page-load', renderAll);
</script>
```

- [ ] **Step 2: 문서 페이지에 주입** — Starlight 컴포넌트 오버라이드(`components: { PageFrame: ... }` 대신 가장 단순한 지점: Starlight config의 `head` 스크립트 or 커스텀 `Footer` 오버라이드에 `<Mermaid />` 포함). 구현자가 Starlight 0.30 docs의 component override 표에서 Footer를 선택해 적용.
- [ ] **Step 3: 검증** — `.spike/target/docs/`에 mermaid 펜스 포함 md 추가 → dev → 해당 페이지에 `<svg` 존재 curl 확인 → 커밋 `feat: client-side mermaid rendering`

---

### Task 14: CLI (`bin/cli.mjs`) + 패키지 메타 (docwatch 개명)

**Files:** Create `bin/cli.mjs`, `tests/unit/cli-args.test.ts` · Modify `package.json`

**Interfaces (Produces):** `npx docwatch [targetDir] [--port N] [--no-open]` — targetDir 기본 `process.cwd()`.

- [ ] **Step 1: 인자 파서를 순수 함수로 분리 + 실패 테스트** (`tests/unit/cli-args.test.ts`)

```ts
import { describe, it, expect } from 'vitest';
import { parseArgs } from '../../bin/cli-args.mjs';

it('기본값: cwd, port 4321, open true', () => {
  expect(parseArgs([], '/cur')).toEqual({ target: '/cur', port: 4321, open: true });
});
it('target 절대화', () => {
  expect(parseArgs(['./proj'], '/cur').target).toBe('/cur/proj');
});
it('--port, --no-open', () => {
  expect(parseArgs(['--port', '5000', '--no-open'], '/c')).toMatchObject({ port: 5000, open: false });
});
```

- [ ] **Step 2: FAIL → 구현** (`bin/cli-args.mjs`)

```js
import path from 'node:path';

export function parseArgs(argv, cwd) {
  let target = cwd, port = 4321, open = true;
  for (let i = 0; i < argv.length; i++) {
    const a = argv[i];
    if (a === '--port') port = Number(argv[++i]);
    else if (a === '--no-open') open = false;
    else if (!a.startsWith('-')) target = path.resolve(cwd, a);
  }
  return { target, port, open };
}
```

- [ ] **Step 3: 엔트리** (`bin/cli.mjs`)

```js
#!/usr/bin/env node
import { spawn } from 'node:child_process';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { parseArgs } from './cli-args.mjs';

const { target, port, open } = parseArgs(process.argv.slice(2), process.cwd());
const pkgRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');

console.log(`[docwatch] watching ${target}`);
const child = spawn('npx', ['astro', 'dev', '--root', pkgRoot, '--port', String(port)], {
  env: { ...process.env, DOCWATCH_ROOT: target, ASTRO_TELEMETRY_DISABLED: '1' },
  stdio: ['ignore', 'pipe', 'inherit'],
});

let opened = false;
child.stdout.on('data', (buf) => {
  const s = buf.toString();
  process.stdout.write(s);
  // gotcha #5: 포트 자동이동 대응 — ready 라인에서 실제 URL 파싱
  const m = s.match(/https?:\/\/(?:localhost|127\.0\.0\.1):(\d+)/);
  if (m && open && !opened) {
    opened = true;
    const url = `http://localhost:${m[1]}/`;
    const cmd = process.platform === 'darwin' ? 'open' : process.platform === 'win32' ? 'start' : 'xdg-open';
    spawn(cmd, [url], { stdio: 'ignore', shell: process.platform === 'win32' }).on('error', () => {});
  }
});
child.on('exit', (code) => process.exit(code ?? 0));
process.on('SIGINT', () => child.kill('SIGINT'));
```

- [ ] **Step 4: package.json 개명·메타** — `"name": "docwatch"`, `"bin": { "docwatch": "./bin/cli.mjs" }`, `"files": ["bin/","src/","public/","astro.config.mjs","tsconfig.json","docwatch.config.ts"]`, `"scripts"`에 `"prepublishOnly": "npm run typecheck && npm run test:unit"` 추가
- [ ] **Step 5: 검증** — `node bin/cli.mjs .spike/target --no-open` → ready 후 curl OK → Ctrl-C 종료 · `npm pack --dry-run`으로 files 화이트리스트 확인 (node_modules·.spike 미포함)
- [ ] **Step 6: 커밋** — `feat: docwatch CLI entry + package rename/bin/files`

---

### Task 15: E2E — 해피 패스 + 라이브 리로드

**Files:** Create `tests/e2e/happy-path.spec.ts`, `tests/e2e/live-reload.spec.ts` · Modify `playwright.config.ts`

- [ ] **Step 1: playwright.config.ts의 webServer를 CLI 경유로**

```ts
import { defineConfig, devices } from '@playwright/test';
import path from 'node:path';

const target = process.env.DOCWATCH_E2E_TARGET ?? path.resolve('.spike/target');

export default defineConfig({
  testDir: 'tests/e2e',
  timeout: 30_000,
  webServer: {
    command: `node bin/cli.mjs ${target} --no-open --port 4321`,
    url: 'http://localhost:4321',
    timeout: 60_000,
    reuseExistingServer: !process.env.CI,
  },
  use: { baseURL: 'http://localhost:4321', ...devices['Desktop Chrome'] },
});
```

- [ ] **Step 2: 해피 패스** (`tests/e2e/happy-path.spec.ts`)

```ts
import { test, expect } from '@playwright/test';

test('대시보드 3구역 렌더', async ({ page }) => {
  await page.goto('/dashboard');
  await expect(page.locator('#live-activity')).toBeVisible();
  await expect(page.locator('#timeline')).toBeVisible();
  await expect(page.locator('#cards')).toBeVisible();
});
test('루트 → /dashboard 리다이렉트', async ({ page }) => {
  await page.goto('/');
  expect(page.url()).toContain('/dashboard');
});
```

- [ ] **Step 3: 라이브 리로드** (`tests/e2e/live-reload.spec.ts`)

```ts
import { test, expect } from '@playwright/test';
import fs from 'node:fs/promises';
import path from 'node:path';

const target = process.env.DOCWATCH_E2E_TARGET ?? path.resolve('.spike/target');
const adr = path.join(target, 'docs/adr/ADR-001.md');

test('외부 파일 수정 → activity 갱신', async ({ request }) => {
  const before = await (await request.get('/api/activity.json')).json();
  await fs.appendFile(adr, '\n- [ ] e2e probe\n');
  await new Promise(r => setTimeout(r, 3000));
  const after = await (await request.get('/api/activity.json')).json();
  expect(after.generatedAt).toBeGreaterThan(before.generatedAt);
  expect(after.recentlyModified.some((x: any) => x.path === 'docs/adr/ADR-001.md')).toBe(true);
});
```

- [ ] **Step 4: 실행** — `npm run test:e2e` → 3건 PASS. 주의: 이 컨테이너의 Chromium lib 이슈 시 CLAUDE.md의 `~/.local/chromium-libs` LD_LIBRARY_PATH 참조.
- [ ] **Step 5: 커밋** — `test: e2e happy path + live reload via CLI`

---

### Task 16: OSS 위생 — README·LICENSE·CONTRIBUTING·CHANGELOG·템플릿

**Files:** Create `README.md`(교체), `LICENSE`, `CONTRIBUTING.md`, `CHANGELOG.md`, `.github/ISSUE_TEMPLATE/bug_report.md`, `.github/ISSUE_TEMPLATE/feature_request.md`, `.github/PULL_REQUEST_TEMPLATE.md`

문서 태스크 (TDD 예외). 내용 요구사항:

- [ ] **README.md**: (1) 최상단 데모 GIF 자리 `![demo](docs/assets/demo.gif)` + TODO 코멘트, (2) 한 줄 설치 `npx docwatch`, (3) "What it does" — 라이브 관전 포지셔닝 (아티팩트 관전 ≠ 토큰 모니터), (4) 지원 컨벤션 표 (docs/, .claude/plans/, .omc/, AGENTS.md, CLAUDE.md, README.md), (5) 옵션 표 (`--port`, `--no-open`, targetDir), (6) `docwatch.config.ts` 오버라이드 예시, (7) **운영 모델 명시**: "docwatch는 상시 실행하는 dev 서버입니다 — 터미널 하나를 점유합니다", (8) Roadmap (v1.1: FilterPanel·git 히스토리·backlinks·mission replay / v1.2: 그래프 뷰·타 에이전트), (9) License MIT
- [ ] **LICENSE**: MIT, copyright 2026 ysj
- [ ] **CONTRIBUTING.md**: dev 셋업 (`npm i` → `npm run dev`는 `.spike/target` 필요 → `bash tests/fixtures/setup-sample-repo.sh` 안내), 테스트 3종 실행법, 커밋 컨벤션, PR 규칙
- [ ] **CHANGELOG.md**: keep-a-changelog 형식, `## [Unreleased]` 섹션
- [ ] **템플릿 3종**: bug(재현 절차·환경·로그), feature(문제·제안·대안), PR(변경 요약·테스트 증거·체크리스트)
- [ ] **커밋** — `docs: OSS hygiene (README, LICENSE, CONTRIBUTING, CHANGELOG, templates)`

---

### Task 17: CI — GitHub Actions (test + release)

**Files:** Create `.github/workflows/test.yml`, `.github/workflows/release.yml`

- [ ] **Step 1: test.yml**

```yaml
name: test
on:
  push: { branches: [main, master] }
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - run: npm run typecheck
      - run: npm run test:unit
      - run: npm run test:integration
      - run: npx playwright install --with-deps chromium
      - run: bash tests/fixtures/setup-sample-repo.sh .spike/target
      - run: npm run test:e2e
        env: { DOCWATCH_E2E_TARGET: .spike/target }
```

- [ ] **Step 2: release.yml** (태그 push 시 npm publish)

```yaml
name: release
on:
  push: { tags: ['v*'] }
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions: { contents: read, id-token: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, registry-url: 'https://registry.npmjs.org' }
      - run: npm ci
      - run: npm publish --provenance --access public
        env: { NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }} }
```

- [ ] **Step 3: yml lint** — `npx yaml-lint .github/workflows/*.yml` 또는 python yaml 파싱으로 문법 확인
- [ ] **Step 4: 커밋** — `build: GitHub Actions test + npm release workflows`

---

### Task 18: 최종 통합 검증 + 브랜치 마무리 준비

수동 검증 태스크 — 구현이 아니라 출시 직전 게이트.

- [ ] **Step 1: 클린 체크아웃 시뮬레이션** — `git stash -u` 후 `npm ci && npm run typecheck && npm run test:unit && npm run test:integration` 전부 PASS 확인, stash pop
- [ ] **Step 2: 실사용 스모크** — `node bin/cli.mjs /home/ysj/Glance --no-open` (실제 프로젝트!) → 대시보드에 Glance의 ADR 9건·AGENTS.md 카테고리 렌더 확인 → 종료
- [ ] **Step 3: `npm pack`** → tarball 내용 검사 (bin/src/config만; .spike·tests·node_modules 없음)
- [ ] **Step 4: `npm view docwatch` 재확인** (이름 선점 여부)
- [ ] **Step 5: 원장(.superpowers/sdd/progress.md) 갱신 + superpowers:finishing-a-development-branch 로 전환** — GitHub 레포 생성·push·태그·publish는 사용자 결정과 토큰이 필요하므로 여기서 STOP하고 사용자에게 보고

---

## Post-plan self-review

**Spec/설계 커버리지**: 설계 문서의 MVP 항목 → ① Starlight 뷰어(T6) ② 대시보드(T8-10) ②-b Live Activity(T5,11,12) ③ 클라 mermaid(T13) ④ npx CLI(T14) + OSS 위생(T16) CI(T17) 출시 게이트(T18). FilterPanel 없음(설계대로 v1.1). 스파이크 gotchas: id 정규화(T2 idToSourceRel), filePath 상대(T2 absFromEntry + T10 사용), 포트 파싱(T14), site 경고(T6 Step3), `.docwatch-cache` exclude(T1 기본값). 에러 방어: git 없음(T9 catch·T5 catch), mermaid 실패시 원본 유지(T13).

**Placeholder 스캔**: TBD/TODO 없음 (README의 데모 GIF TODO는 의도된 산출물 지시). 모든 코드 스텝에 실제 코드 포함.

**타입 일관성**: `DocwatchConfig`/`CategoryRule`(T1) ↔ T2·T5·T6 사용처 일치. `Commit`/`DirtyEntry`(T4) ↔ T5·T9·T12. `Activity`(T5) ↔ T11·T12. `parseArgs`(T14) 시그니처 테스트와 일치. `aggregateByCategory`는 `sourceRelPath` 키 사용 — T10에서 target 기준 상대경로로 공급 (id 아님, 카테고리 규칙이 원본 경로 기반이므로).

**실행 순서 의존성**: T1→T2(타입)→T3→T4→T5(T3·T4 소비)→T6(T1·T2)→T7(T2)→T8→T9(T4)→T10(T5·T2)→T11(T5)→T12(T5)→T13 독립→T14(전체 러너)→T15(T14)→T16·T17 독립→T18 최종. 병렬 여지: T13·T16·T17은 앞 태스크와 독립.

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-07-07-docwatch.md`.**

1. **Subagent-Driven (recommended)** — 태스크별 fresh 구현자 (CLAUDE.md 라우팅: implementer=`codex:codex-rescue` 또는 direct codex exec, reviewer=`oh-my-claudecode:code-reviewer`)
2. **Inline Execution** — superpowers:executing-plans, 구현은 여전히 Codex 위임

Which approach?
